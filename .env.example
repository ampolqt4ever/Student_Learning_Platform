# Where the SignMath backend (see /signmath-backend) is running.
# Copy this file to .env.local and adjust if you're not running locally on the default port.
VITE_BACKEND_WS_URL=ws://localhost:8000/ws/predict
VITE_BACKEND_HTTP_URL=http://localhost:8000
